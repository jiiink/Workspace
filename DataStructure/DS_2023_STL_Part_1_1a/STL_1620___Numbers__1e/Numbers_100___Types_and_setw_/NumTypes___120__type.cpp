#include <bits/stdc++.h>
using namespace std ;


int main() {

   int      ci = 0;
   float    cf = 0.0;
   double   cd = 0.0;

   cout << typeid(ci).name() << '\n';
   cout << typeid(cf).name() << '\n';
   cout << typeid(cd).name() << '\n';
}
